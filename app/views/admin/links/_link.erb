<li>
  <h3><%=  link_to link.url, link.url, :target => "_blank" %></h3>
  <div class="except"><%= link.description %></div>
  <div class="actions">
    <%= link_to 'Edit', [:admin, @post, link] %>
    <%= link_to 'Remove', [:admin, @post, link], :method => :delete, 
         :confirm => "Are you sure?" %>
  </div>
</li>